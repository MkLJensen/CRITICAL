class MetalProcess is subclass of RecyclingProcess


operations

public action : (GarbageType) ==> ()
action(o) == (
    IO`print("Recycling Metal with Weight: ");
    IO`print(o.getWeight());
    IO`print("With Dimensions: ");
    IO`print(o.getVolume());
    IO`print("Melting");
);

end MetalProcess