class World
instance variables  

public static
  env : [Environment] := nil;

public static 
  timer : Timer := new Timer();
   
operations

public 
  World : () ==> World
  World() ==
  (
    env := new Environment("scenario2.txt");
  );
  
public 
  Run : () ==> ()
  Run() ==
    env.Run();

end World